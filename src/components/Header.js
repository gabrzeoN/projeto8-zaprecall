import Logo from "./../assets/img/logo.png";

export default function Main() {
    return (
        <header>
            <img src={Logo} alt="ZapRecall logo" />
            <h1>ZapRecall</h1>
        </header>
    );
}